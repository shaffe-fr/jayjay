[package]
name = "jayjay-gpui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license = "BUSL-1.1"
publish = false

[[bin]]
name = "jayjay-gpui"
path = "src/main.rs"

[target.'cfg(windows)'.build-dependencies]
winresource = "0.1"

[dependencies]
jayjay-core = { path = "../../crates/jayjay-core" }
jayjay-markdown = { path = "../../crates/jayjay-markdown" }
jayjay-network = { path = "../../crates/jayjay-network", features = ["blocking"] }
jayjay-review = { path = "../../crates/jayjay-review" }
gpui = { git = "https://github.com/zed-industries/zed", tag = "v1.20.2" }
# font-kit/runtime_shaders are macOS-only forwards; wayland/x11 enable the Linux
# windowing backends (no-ops elsewhere). Without them the Linux binary cannot open a window.
gpui_platform = { git = "https://github.com/zed-industries/zed", tag = "v1.20.2", features = ["font-kit", "runtime_shaders", "wayland", "x11"] }
chrono = { workspace = true }
clap = { workspace = true }
sha2 = { workspace = true }
uuid = { workspace = true }
percent-encoding = { workspace = true }
base64 = { workspace = true }
image = { workspace = true }
resvg = "0.46"
font-kit = "0.14"
hex = "0.4"
notify = "8"
open = "5"
flume = "0.12"
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
unicode-segmentation = { workspace = true }
unicode-width = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
font-kit = { version = "0.14", features = ["source-fontconfig-default"] }
libc = { workspace = true }
notify-rust = "4.18.0"

[dev-dependencies]
gpui = { git = "https://github.com/zed-industries/zed", tag = "v1.20.2", features = ["test-support"] }
jj-test = { path = "../../crates/jj-test" }
tempfile = { workspace = true }
