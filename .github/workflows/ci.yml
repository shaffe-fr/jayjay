name: CI

# Disabled on this Windows fork: macOS/Swift CI runs manually only.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: stable

      - name: Install jj (for component test fixtures)
        run: brew install jj

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Test
        run: cargo test --workspace --exclude jayjay-gpui

      # GPUI tests run on Linux and Windows in GPUI CI; macOS only proves the development build still compiles.
      - name: Check GPUI builds on macOS
        run: cargo check -p jayjay-gpui

  swift:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: stable

      - name: Install tools
        run: brew install just xcodegen xcbeautify swiftlint jj

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          # The swift and swift-ui jobs build the same UniFFI artifacts; one shared entry keeps the repo under the 10 GB cache limit.
          shared-key: swift
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Lint Swift
        run: swiftlint lint --config .swiftlint.yml --strict

      - name: Build app
        run: just build

      - name: Test app
        run: just test-app

  swift-ui-build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: stable

      - name: Install tools
        run: brew install just xcodegen xcbeautify

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: swift
          save-if: false

      - name: Build app and UI test bundle
        run: just shell::ui-test-build

      # upload-artifact drops file modes, so the bundles travel as a tarball.
      - name: Pack build products
        run: tar -C build/DerivedData/Build -czf ui-test-products.tgz Products

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ui-test-products
          path: ui-test-products.tgz
          retention-days: 1
          if-no-files-found: error

  swift-ui:
    name: swift-ui (${{ matrix.shard }}/3)
    needs: swift-ui-build
    runs-on: macos-26
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install tools
        run: brew install just xcbeautify jj

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ui-test-products

      - name: Unpack build products
        run: mkdir -p build/DerivedData/Build && tar -C build/DerivedData/Build -xzf ui-test-products.tgz

      - name: UI tests
        run: just test-ui-shard ${{ matrix.shard }} 3 shell::ui-test-prebuilt

      - name: Upload xcresult on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ui-test-xcresult-${{ matrix.shard }}
          path: build/DerivedData/Logs/Test/*.xcresult
          if-no-files-found: warn
